---
title: CLI Commands
description: Command-line interface tools for Glueful administration
navigation:
  icon: i-lucide-terminal
---

Glueful comes with a powerful command-line interface (CLI) that provides tools for application management, development, and maintenance. The CLI makes it easy to perform common tasks without needing to access the web interface.

## Basic Usage

All Glueful CLI commands follow this structure:

```bash
php glueful <command> [options] [arguments]
```

For example:

````bash
# Show all available commands
php glueful list

# Get help for a specific command
php glueful help <command>
```

## Help System

The help system provides comprehensive documentation for all available commands.

```bash
# List all available commands
php glueful list

# Get detailed help for a specific command
php glueful help <command>

# Examples
php glueful help migrate:run
php glueful help generate:key
php glueful help extensions:create
```

## Installation & Setup Commands

Bootstrap your Glueful application and configure the environment.

```bash
# Install Glueful framework (interactive setup wizard)
php glueful install

# Install with skip options
php glueful install --skip-db --skip-keys --skip-admin

# Force overwrite existing configurations
php glueful install --force

# Run without interactive prompts
php glueful install --quiet

# Generate application keys
php glueful generate:key

# Generate JWT key only
php glueful generate:key --jwt-only

# Generate application key only
php glueful generate:key --app-only

# Force overwrite existing keys
php glueful generate:key --force

# Display generated keys (insecure - for debugging only)
php glueful generate:key --show

# Check system requirements and configuration
php glueful system:check

# Check with detailed information
php glueful system:check --verbose

# Check production readiness
php glueful system:check --production

# Attempt to fix common issues
php glueful system:check --fix

# Validate production environment
php glueful system:production

# Check with detailed output
php glueful system:production --verbose

# Apply production fixes
php glueful system:production --fix

# Generate production audit report
php glueful system:production --audit
```

## Development Server Commands

Start and manage development servers.

```bash
# Start development server (default: localhost:8000)
php glueful serve

# Start with custom port
php glueful serve --port=8080

# Start with custom host and port
php glueful serve --host=0.0.0.0 --port=8080
```

## Database Commands

Database management tools for migrations, schema, and maintenance.

### Migration Commands

```bash
# Run pending migrations
php glueful migrate:run

# Run migrations with specific batch number
php glueful migrate:run --batch=2

# Run migrations from custom directory
php glueful migrate:run --path=/custom/migrations

# Create a new migration file
php glueful migrate:create create_tasks_table

# Rollback migrations (last batch by default)
php glueful migrate:rollback

# Rollback specific number of migration steps
php glueful migrate:rollback --steps=3

# Show migration status
php glueful migrate:status

# Dry run (show what would be executed)
php glueful migrate:run --dry-run
php glueful migrate:rollback --pretend
```

### Database Status & Management

```bash
# Check database connection status and statistics
php glueful db:status

# Reset database (requires confirmation)
php glueful db:reset --force

# Note: db:fresh and db:seed commands are not available in current version
# Use migrate:run and custom seeding scripts instead

```

### Schema Commands

```bash
# Generate API definitions from database
php glueful generate:api-definitions

# Generate with specific database
php glueful generate:api-definitions --database=mydb

# Generate for specific table
php glueful generate:api-definitions --database=mydb --table=users

# Force overwrite existing definitions
php glueful generate:api-definitions --force
```

### DI Container Commands

```bash
# Compile container for production optimization
php glueful di:container:compile

# Debug and inspect container services
php glueful di:container:debug

# Show specific service details
php glueful di:container:debug --service=UserService

# Validate container configuration
php glueful di:container:validate
```

### Configuration Commands

```bash
# Validate configuration files against schemas
php glueful config:validate

# Generate configuration documentation
php glueful config:generate-docs

# Generate IDE support files for auto-completion
php glueful config:generate-ide-support
```

### Route Management Commands

```bash
# List all registered routes
php glueful route list

# Cache routes for production
php glueful route cache

# Clear route cache
php glueful route clear
```

### Query & Performance Commands

```bash
# Database query profiling with detailed analysis
php glueful db:profile

# Profile specific query
php glueful db:profile --query="SELECT * FROM users"

# Profile with execution plan
php glueful db:profile --explain

# Profile query from file
php glueful db:profile --file=query.sql

# Output in specific format
php glueful db:profile --output=json
```

### Code Generation Commands

```bash
# Generate a REST API controller
php glueful generate:controller TaskController

# Generate a resource controller with full CRUD methods
php glueful generate:controller UserController --resource

# Generate an API-only controller (no create/edit views)
php glueful generate:controller ApiController --api

# Force overwrite existing files
php glueful generate:controller TaskController --force

# Combine options for resource API controller
php glueful generate:controller UserApiController --resource --api

# Note: Model, middleware, service, and repository generators are not available in current version
# Create these manually using the provided templates in the documentation
```

## Cache Commands

Cache management for performance optimization.

```bash
# Clear all cached data
php glueful cache:clear

# Alias for clear
php glueful cache:flush

# Show cache status and statistics
php glueful cache:status

# Get cached item by key
php glueful cache:get <key>

# Set cache item with key, value and TTL (default: 3600 seconds)
php glueful cache:set <key> <value> [ttl]

# Delete cached item by key or pattern
php glueful cache:delete <key>

# Get TTL for cached item
php glueful cache:ttl <key>

# Set new TTL for cached item
php glueful cache:expire <key> <seconds>

# Edge cache management
php glueful cache:purge                   # Purge edge cache content
```

## Extension Commands

Complete extension lifecycle management.

```bash
# List all installed extensions
php glueful extensions:info

# Show detailed extension information
php glueful extensions:info <extension-name>

# Enable/disable extensions
php glueful extensions:enable <extension-name>
php glueful extensions:disable <extension-name>

# Create new extension from template
php glueful extensions:create <extension-name>

# Install extension from source
php glueful extensions:install <source> [target-name]

# Validate extension structure
php glueful extensions:validate <extension-name>

# Debug extension system
php glueful extensions:debug

# Performance benchmarking
php glueful extensions:benchmark

# Delete extension
php glueful extensions:delete <extension-name>
```

## Notification Commands

Manage notification system and process retries.

```bash
# Process queued notification retries
php glueful notifications:process-retries

# Process with custom limit
php glueful notifications:process-retries --limit=100
```

## Queue Commands

Unified queue management system with comprehensive monitoring.

```bash
# Worker management
php glueful queue:work                                 # Start processing queue jobs
php glueful queue:work --daemon --memory=512          # Run as daemon with memory limit

# Auto-scaling management
php glueful queue:autoscale                            # Advanced queue auto-scaling

# Job scheduling
php glueful queue:scheduler                            # Advanced job scheduling and management
```

## Archive Commands

Data archiving system for retention and compliance.

```bash
# Comprehensive archive system management
php glueful archive:manage                             # Archive system management and operations

# Use subcommands for specific operations:
# - archive <table> [days]                             # Archive table data
# - search <table> <query>                             # Search archived data
# - status                                             # Show archive system status
# - summary                                            # Show archive summary statistics
# - health                                             # Check archive system health
# - cleanup                                            # Clean old archived data
# - verify <table>                                     # Verify archive integrity
```

## Memory Management Commands

Monitor and optimize memory usage.

```bash
# Advanced memory monitoring and analysis
php glueful system:memory                              # Memory monitoring tools
php glueful system:memory --interval=1 --threshold=20 # Monitor with custom settings
php glueful system:memory --duration=60               # Monitor for specific duration
php glueful system:memory <command>                   # Monitor specific command execution

# Examples
php glueful system:memory "php glueful migrate:run"
php glueful system:memory "php glueful cache:clear"
```

## Security Commands

Comprehensive security management and monitoring.

```bash
# Security scanning and validation
php glueful security:check                             # Check security configuration
php glueful security:scan                              # Run vulnerability scan
php glueful security:report                            # Generate comprehensive security report
php glueful security:vulnerabilities                   # Check for known vulnerabilities

# Token management
php glueful security:revoke-tokens <user_uuid>         # Revoke user tokens
php glueful security:revoke-tokens --all               # Revoke all tokens (emergency)

# User security operations  
php glueful security:reset-password <user_uuid>        # Force password reset

# Emergency lockdown
php glueful security:lockdown                          # Manage emergency lockdown mode
php glueful security:lockdown --enable                 # Enable lockdown
php glueful security:lockdown --disable                # Disable lockdown
```

## System Commands

System health and maintenance commands.

```bash
# System validation and setup
php glueful system:check                               # System health check
php glueful system:check --verbose                     # Detailed system check
php glueful system:check --production                  # Production readiness check

# Security key management
php glueful generate:key                               # Generate security keys
php glueful generate:key --force                       # Force regenerate keys

# Production configuration
php glueful system:production                          # Production environment validation
php glueful system:production --check                  # Check production readiness
php glueful system:production --audit                  # Generate audit report

# API documentation generation
php glueful generate:api-definitions                   # Generate API definitions
```

## Scheduler Commands

Task scheduling and background job management (if implemented in your extensions).

```bash
# Note: Scheduler commands are not available in the core framework
# Use queue:scheduler for job scheduling:
php glueful queue:scheduler                            # Advanced job scheduling

# For cron-based scheduling, integrate directly with system cron
# See the "Cron Integration" section below
```


## Command Categories

### Core Commands

- `list` - List all available commands
- `help` - Show detailed help for commands
- `install` - Install and bootstrap Glueful framework
- `serve` - Start development server
- `system:check` - Validate system requirements
- `system:production` - Production configuration management

### Authentication & Security

- `generate:key` - Generate secure encryption keys
- `security:check` - Check security configuration
- `security:scan` - Run vulnerability scans
- `security:report` - Generate security reports
- `security:vulnerabilities` - Check for known vulnerabilities
- `security:lockdown` - Emergency lockdown mode management
- `security:revoke-tokens` - Token revocation management
- `security:reset-password` - Force password reset

### Database Management

- `migrate:create` - Create new migration file
- `migrate:run` - Execute pending migrations
- `migrate:rollback` - Revert migrations
- `migrate:status` - Show migration status
- `db:status` - Check database connection and statistics
- `db:reset` - Reset database (destructive operation)
- `db:profile` - Database query profiling with detailed analysis
- `generate:api-definitions` - Generate API definitions from database
- `generate:controller` - Generate REST API controllers

### DI Container Management

- `di:container:compile` - Compile the DI container for production
- `di:container:debug` - Debug and inspect container services
- `di:container:validate` - Validate container configuration

### Configuration Management

- `config:validate` - Validate configuration files against schemas
- `config:generate-docs` - Generate configuration documentation
- `config:generate-ide-support` - Generate IDE support files

### Cache Operations

- `cache:clear` / `cache:flush` - Clear all cache
- `cache:status` - Show cache statistics
- `cache:get` - Get cached item by key
- `cache:set` - Set cache item with TTL
- `cache:delete` - Delete cached item
- `cache:ttl` - Get item TTL
- `cache:expire` - Set item TTL
- `cache:purge` - Edge cache purging

### Extension Management

- `extensions:info` - List extensions or show details
- `extensions:enable` - Enable extension
- `extensions:disable` - Disable extension
- `extensions:create` - Create new extension
- `extensions:install` - Install extension from source
- `extensions:validate` - Validate extension structure
- `extensions:delete` - Delete extension
- `extensions:benchmark` - Performance benchmarking
- `extensions:debug` - Debug extension system

### Queue System

- `queue:work` - Process queue jobs with daemon support
- `queue:autoscale` - Advanced queue auto-scaling
- `queue:scheduler` - Job scheduling and management

### Archive System

- `archive:manage` - Comprehensive archive system management
  - Subcommands: archive, search, status, summary, health, cleanup, verify

### Notification System

- `notifications:process-retries` - Process queued notification retries

### Memory Management

- `system:memory` - Advanced memory monitoring and analysis tools

### Security Management

- `security:check` - Security configuration validation
- `security:scan` - Vulnerability scanning
- `security:report` - Generate security reports
- `security:vulnerabilities` - Check for known vulnerabilities
- `security:revoke-tokens` - Token management and revocation
- `security:reset-password` - Force password reset
- `security:lockdown` - Emergency lockdown procedures

### System Administration

- `system:check` - System health validation and production readiness
- `system:production` - Production configuration management
- `system:memory` - Memory monitoring and analysis
- `generate:key` - Security key generation
- `route` - Route management (cache, clear, list)

## Command Options & Flags

Most commands support common options:

```bash
# Get help for any command
php glueful help <command>

# Force execution (when supported)
php glueful <command> --force

# Verbose output (when supported)
php glueful <command> --verbose

# Common options available on most commands:
# --silent         Do not output any message
# --quiet          Only errors are displayed
# --no-interaction Do not ask any interactive question
# --ansi/--no-ansi Force (or disable) ANSI output
```

## Environment-Specific Commands

Some commands behave differently based on environment:

```bash
# Production environment checks
php glueful system:check --production
php glueful config:production --check

# Development helpers
php glueful serve --port=8080
php glueful memory:monitor --interval=1
```

## Creating Custom Commands

You can extend Glueful's CLI functionality by creating your own custom commands.

### 1. Create Command Class

Create a new command class that extends `Glueful\Console\Command`:

```php
<?php

namespace App\Commands;

use Glueful\Console\Command;

class MyCustomCommand extends Command
{
    /**
     * Get Command Name
     */
    public function getName(): string
    {
        return 'my:command';
    }

    /**
     * Get Command Description
     */
    public function getDescription(): string
    {
        return 'Description of my custom command';
    }

    /**
     * Get detailed help text
     */
    public function getHelp(): string
    {
        return <<<HELP
Custom Command Help

Usage:
  php glueful my:command [options] [arguments]

Options:
  --option1    Description of option 1
  --option2    Description of option 2

Examples:
  php glueful my:command
  php glueful my:command --option1=value
HELP;
    }

    /**
     * Execute the command
     */
    public function execute(array $args = []): int
    {
        $this->info('Executing my custom command...');

        // Parse arguments and options
        $options = $this->parseOptions($args);

        // Your command logic here
        try {
            // Implementation
            $this->success('Command completed successfully');
            return Command::SUCCESS;
        } catch (\Exception $e) {
            $this->error('Command failed: ' . $e->getMessage());
            return Command::FAILURE;
        }
    }

    /**
     * Parse command options
     */
    private function parseOptions(array $args): array
    {
        $options = [];
        foreach ($args as $arg) {
            if (strpos($arg, '--') === 0) {
                $parts = explode('=', substr($arg, 2), 2);
                $options[$parts[0]] = $parts[1] ?? true;
            }
        }
        return $options;
    }
}
```

### 2. Register Command

Register your command in the console kernel:

```php
// In your bootstrap file or service provider
use Glueful\Console\Kernel;

$kernel = new Kernel();
$kernel->registerCommand(\App\Commands\MyCustomCommand::class);
```

### 3. Advanced Command Features

```php
class AdvancedCommand extends Command
{
    public function execute(array $args = []): int
    {
        // Input validation
        if (empty($args)) {
            $this->error('Missing required argument');
            return Command::INVALID;
        }

        // Progress indication
        $this->info('Starting operation...');

        // Table output
        $this->table(
            ['ID', 'Name', 'Status'],
            [
                [1, 'Item 1', 'Active'],
                [2, 'Item 2', 'Inactive']
            ]
        );

        // Colored output
        $this->success('Operation completed');
        $this->warning('Warning message');
        $this->error('Error message');

        return Command::SUCCESS;
    }
}
```

## Console Output Formatting

The Command base class provides comprehensive formatting options:

### Basic Output Methods

```php
// Display messages with different severity levels
$this->info('Information message');
$this->success('Success message in green');
$this->warning('Warning message in yellow');
$this->error('Error message in red');
$this->line('Plain text line');
```

### Advanced Formatting

```php
// Display tables
$this->table(
    ['Column 1', 'Column 2', 'Column 3'],
    [
        ['Row 1 Col 1', 'Row 1 Col 2', 'Row 1 Col 3'],
        ['Row 2 Col 1', 'Row 2 Col 2', 'Row 2 Col 3']
    ]
);

// Progress indicators
$this->info('Processing items...');
for ($i = 0; $i < 100; $i++) {
    // Show progress
    $this->line("Progress: " . ($i + 1) . "/100");
}
```

### Color and Styling

```php
// Manual color control
echo "\033[32mGreen text\033[0m\n";
echo "\033[33mYellow text\033[0m\n";
echo "\033[31mRed text\033[0m\n";
echo "\033[36mCyan text\033[0m\n";
```

## Command Exit Codes

Glueful follows standard Unix exit codes for command results:

- `Command::SUCCESS (0)` - Command executed successfully
- `Command::FAILURE (1)` - General failure during command execution
- `Command::INVALID (2)` - Invalid command usage or input

Example usage in commands:

```php
public function execute(array $args = []): int
{
    // Validate input
    if (!$this->validateInput($args)) {
        $this->error('Invalid input provided');
        return Command::INVALID;
    }

    try {
        // Execute command logic
        $this->performOperation();
        $this->success('Operation completed');
        return Command::SUCCESS;
    } catch (\Exception $e) {
        $this->error('Operation failed: ' . $e->getMessage());
        return Command::FAILURE;
    }
}
```

## Command Debugging

### Debug Mode

Enable verbose output for troubleshooting:

```bash
# Enable debug output
php glueful <command> --verbose

# Combine with specific commands
php glueful system:check --verbose
php glueful extensions:install MyExtension --verbose
```

### Memory Monitoring

Monitor command memory usage:

```bash
# Monitor memory while running command
php glueful memory:monitor php glueful migrate run

# Monitor with logging
php glueful memory:monitor --log --csv=migration-memory.csv php glueful migrate run
```

## Available Commands Reference

Based on the current Glueful CLI implementation (v1.0.0), here are all available commands organized by namespace:

### Core Commands

- `completion` - Dump the shell completion script
- `help` - Display help for a command
- `install` - Run installation setup wizard
- `list` - List all available commands
- `route` - Manage application routes (cache, clear, list)
- `serve` - Start the development server

### Archive Commands (archive:*)

- `archive:manage` - Comprehensive archive system management

### Cache Commands (cache:*)

- `cache:clear` / `cache:flush` - Clear application cache
- `cache:delete` - Delete cached entries by key or pattern
- `cache:expire` - Set new TTL for cached items
- `cache:get` - Get a cached value by key
- `cache:purge` - Purge edge cache content
- `cache:set` - Set a cache value with optional TTL
- `cache:status` - Show cache system status
- `cache:ttl` - Get TTL for cached items

### Configuration Commands (config:*)

- `config:generate-docs` - Generate configuration documentation
- `config:generate-ide-support` - Generate IDE support files
- `config:validate` - Validate configuration files

### Database Commands (db:*)

- `db:profile` - Profile and analyze database queries
- `db:reset` - Reset database (drops all tables)
- `db:status` - Show database connection status

### DI Container Commands (di:*)

- `di:container:compile` - Compile DI container for production
- `di:container:debug` - Debug and inspect container services
- `di:container:validate` - Validate container configuration

### Extension Commands (extensions:*)

- `extensions:benchmark` - Run performance benchmarks
- `extensions:create` - Create new extension from template
- `extensions:debug` - Show debug information
- `extensions:delete` - Delete an extension completely
- `extensions:disable` - Disable an active extension
- `extensions:enable` - Enable an installed extension
- `extensions:info` - Display extension information
- `extensions:install` - Install extension from source
- `extensions:validate` - Validate extension structure

### Generator Commands (generate:*)

- `generate:api-definitions` - Generate OpenAPI documentation
- `generate:controller` - Generate REST API controller
- `generate:key` - Generate secure encryption keys

### Migration Commands (migrate:*)

- `migrate:create` - Create new migration file
- `migrate:rollback` - Rollback database migrations
- `migrate:run` - Run pending migrations
- `migrate:status` - Show migration status

### Notification Commands (notifications:*)

- `notifications:process-retries` - Process queued notification retries

### Queue Commands (queue:*)

- `queue:autoscale` - Advanced queue auto-scaling
- `queue:scheduler` - Job scheduling and management
- `queue:work` - Start queue workers

### Security Commands (security:*)

- `security:check` - Check security configuration
- `security:lockdown` - Manage emergency lockdown mode
- `security:report` - Generate security report
- `security:reset-password` - Force password reset
- `security:revoke-tokens` - Revoke authentication tokens
- `security:scan` - Scan for vulnerabilities
- `security:vulnerabilities` - Check for known vulnerabilities

### System Commands (system:*)

- `system:check` - Validate framework installation
- `system:memory` - Advanced memory monitoring tools
- `system:production` - Production environment configuration

## Command Usage Examples

```bash
# Get help for any specific command
php glueful help migrate
php glueful extensions help

# Common workflows
php glueful install                           # Initial setup
php glueful generate:key --force              # Generate new keys
php glueful system:check --production         # Production readiness
php glueful migrate:run                       # Update database
php glueful cache:clear                       # Clear application cache
php glueful extensions:info                   # View installed extensions
php glueful queue:scheduler                   # Manage scheduled jobs

# Database workflow
php glueful migrate:create add_user_profile_table
php glueful migrate:run
php glueful migrate:status

# Code generation workflow
php glueful generate:controller UserController --resource
php glueful generate:api-definitions

# Extension workflow
php glueful extensions:create MyExtension
php glueful extensions:enable MyExtension
php glueful extensions:info MyExtension

# Cache management workflow
php glueful cache:status
php glueful cache:set user:123 "John Doe" 3600
php glueful cache:get user:123
php glueful cache:clear

# Development workflow
php glueful serve --port=8080
php glueful system:memory "php glueful migrate:run"
php glueful db:profile --explain
```

### Batch Operations

Create shell scripts for common operations:

```bash
#!/bin/zsh
# deploy.sh - Deployment script

echo "Starting deployment..."

# Run database migrations
php glueful migrate:run

# Clear application cache
php glueful cache:clear

# Purge edge cache
php glueful cache:purge --all

# Process notification retries
php glueful notifications:process-retries

# Check system health
php glueful system:check --production

echo "Deployment completed"
```

```bash
#!/bin/zsh
# development-setup.sh - Development environment setup

echo "Setting up development environment..."

# Install Glueful
php glueful install --skip-admin

# Generate keys
php glueful generate:key --force

# Run migrations
php glueful migrate:run

# Check system
php glueful system:check --verbose

echo "Development environment ready!"
```

### Scheduler Integration

The scheduler command can be used for automated task execution:

```bash
# Use queue scheduler for job scheduling
php glueful queue:scheduler

# Run queue workers with daemon mode
php glueful queue:work --daemon --memory=512

# Configure auto-scaling
php glueful queue:autoscale
```

### Cron Integration

For production environments, integrate with system cron:

```bash
# Add to crontab (crontab -e)
# Run queue workers (if not using daemon mode)
*/5 * * * * cd /path/to/glueful && php glueful queue:work >> /var/log/glueful-queue.log 2>&1

# Process notification retries every 10 minutes
*/10 * * * * cd /path/to/glueful && php glueful notifications:process-retries >> /var/log/glueful-notifications.log 2>&1

# Clear cache daily at 3 AM
0 3 * * * cd /path/to/glueful && php glueful cache:clear >> /var/log/glueful-maintenance.log 2>&1
```

## Best Practices

### Command Design

- **Single Responsibility**: Each command should have one clear purpose
- **Descriptive Names**: Use clear, consistent naming (e.g., `namespace:action`)
- **Comprehensive Help**: Provide detailed help text with examples
- **Error Handling**: Always handle exceptions and return appropriate exit codes

### Performance

- **Memory Awareness**: Monitor memory usage with `php glueful memory:monitor`
- **Progress Feedback**: Commands show progress for long-running operations
- **Database Optimization**: Use `query:profile` for performance analysis
- **Cache Management**: Regular cache clearing for optimal performance

### Security

- **Input Validation**: Commands validate and sanitize arguments
- **Safe Defaults**: Destructive operations require explicit confirmation (e.g., `--force`)
- **Key Management**: Secure key generation with `key:generate`
- **Production Checks**: Use `config:production` for production readiness

### Development Workflow

- **Use Help System**: Explore `php glueful list` and `php glueful help <command>` to discover available functionality
- **Test Commands**: Test commands in development before production use
- **System Validation**: Use `system:check` to verify environment setup
- **Monitor Resources**: Use `memory:monitor` for resource-intensive operations
- **Migration Management**: Use proper migration workflow (create → run → status)

### Production Considerations

- **Backup First**: Always backup data before running destructive commands like `db:reset`
- **Production Checks**: Use `system:check --production` and `config:production` for production readiness
- **Schedule Tasks**: Use `scheduler` for routine maintenance tasks
- **Monitor Performance**: Use profiling tools for database and memory optimization
- **Automate Deployments**: Create deployment scripts for consistent deployments
- **Log Management**: Redirect command output to logs for monitoring
- **Environment-Specific Configs**: Use appropriate flags for different environments

### Common Workflows

#### Initial Setup

```bash
php glueful install
php glueful generate:key
php glueful system:check
```

#### Database Management

```bash
php glueful migrate:create <migration_name>
php glueful migrate:run
php glueful migrate:status
```

#### Production Deployment

```bash
php glueful migrate:run
php glueful cache:clear
php glueful system:production --check
php glueful system:check --production
```

#### Extension Development

```bash
php glueful extensions:create <extension_name>
php glueful extensions:validate <extension_name>
php glueful extensions:enable <extension_name>
```

## Troubleshooting

### Common Issues

#### Command Not Found

```bash
# Error: php glueful: command not found
# Solution: Make sure you're in the correct directory
cd /path/to/your/glueful/project
php glueful help

# Or use absolute path
php /path/to/glueful/glueful help
```

#### Permission Issues

```bash
# Error: Permission denied
# Solution: Check file permissions
chmod +x glueful
chmod -R 755 storage/
chmod -R 755 database/migrations/
```

#### Database Connection Issues

```bash
# Check database status
php glueful db:status

# Verify system configuration
php glueful system:check --verbose

# Test database connection in install
php glueful install --skip-keys --skip-admin
```

#### Migration Issues

```bash
# Check migration status
php glueful migrate:status

# Reset migrations (WARNING: destructive)
php glueful db:reset --force
php glueful migrate:run
```

#### Cache Issues

```bash
# Clear all cache
php glueful cache:clear

# Check cache status
php glueful cache:status

# Verify cache configuration
php glueful system:check
```

#### Extension Issues

```bash
# List all extensions
php glueful extensions:info

# Validate extension structure
php glueful extensions:validate <extension-name>

# Check extension information
php glueful extensions:info <extension-name>

# Re-enable extension
php glueful extensions:disable <extension-name>
php glueful extensions:enable <extension-name>
```

### Debug Mode

Enable verbose output for detailed troubleshooting:

```bash
# Most commands support verbose output
php glueful system:check --verbose
php glueful install --verbose
php glueful migrate run --dry-run

# Monitor memory during command execution
php glueful memory:monitor php glueful migrate run

# Monitor with logging
php glueful memory:monitor --log --csv=migration-memory.csv php glueful migrate run
```

### Log Files

Check relevant log files for detailed error information:

```bash
# Application logs (if configured)
tail -f storage/logs/app.log

# System logs
tail -f /var/log/syslog  # Linux
tail -f /var/log/system.log  # macOS

# Web server logs
tail -f /var/log/apache2/error.log  # Apache
tail -f /var/log/nginx/error.log    # Nginx
```

### Environment Issues

```bash
# Check PHP version and extensions
php glueful system:check

# Verify .env configuration
php glueful config:production --check

# Test production readiness
php glueful system:check --production
```

### Getting Help

```bash
# General help
php glueful help

# Command-specific help
php glueful <command> help
php glueful help <command>

# List all available commands
php glueful help | grep "Available commands:" -A 20
```
