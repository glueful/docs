---
title: CLI Commands
description: Command-line interface tools for Glueful administration
navigation:
  icon: i-lucide-terminal
---

Glueful comes with a powerful command-line interface (CLI) that provides tools for application management, development, and maintenance. The CLI makes it easy to perform common tasks without needing to access the web interface.

## Basic Usage

All Glueful CLI commands follow this structure:

```bash
php glueful <command> [options] [arguments]
```

For example:

```bash
# Show all available commands
php glueful help

# Get help for a specific command
php glueful help db:migrate
```

## Help System

The help system provides comprehensive documentation for all available commands.

```bash
# List all available commands
php glueful help

# Get detailed help for a specific command
php glueful help <command>
```

## Installation & Setup Commands

Bootstrap your Glueful application and configure the environment.

```bash
# Install Glueful framework
php glueful install

# Generate application key
php glueful key:generate

# Check system requirements and configuration
php glueful system:check

# Check production readiness
php glueful system:check --production

# Configure production environment
php glueful config:production
```

## Development Server Commands

Start and manage development servers.

```bash
# Start development server
php glueful serve

# Start with custom port
php glueful serve --port=8080

# Start with custom host
php glueful serve --host=0.0.0.0
```

## Database Commands

Comprehensive database management tools for migrations, schema, and maintenance.

### Migration Commands

```bash
# Run pending migrations
php glueful db:migrate

# Check database migration status
php glueful db:status

# Reset database (requires confirmation)
php glueful db:reset --force

# Rollback last migration
php glueful db:rollback

# Refresh database (reset and migrate)
php glueful db:refresh
```

### Schema Commands

```bash
# Generate JSON schema from database
php glueful generate:json api-definitions -d mydb -T users

# Export database schema
php glueful db:schema-export

# Import database schema
php glueful db:schema-import --file=schema.sql

# Show database structure
php glueful db:structure
```

### Query & Performance Commands

```bash
# Profile database queries
php glueful query:profile

# Analyze slow queries
php glueful query:analyze

# Show query statistics
php glueful query:stats

# Optimize database tables
php glueful db:optimize
```

## Cache Commands

Advanced cache management for performance optimization.

```bash
# Clear all cached data
php glueful cache clear

# Show cache status and statistics
php glueful cache status

# Interact with cache items
php glueful cache get <key>
php glueful cache set <key> <value> [<ttl>]
php glueful cache delete <key>

# Set TTL for cached item
php glueful cache ttl <key>
php glueful cache expire <key> <seconds>

# Warm up cache
php glueful cache:warmup

# Clear specific cache tags
php glueful cache:clear-tag <tag>
```

### Edge Cache Commands

```bash
# Purge edge cache
php glueful edge:purge

# Purge specific URLs
php glueful edge:purge --urls=/api/users,/api/posts

# Purge by cache tags
php glueful edge:purge --tags=users,posts
```

## Extension Commands

Complete extension lifecycle management.

```bash
# List all installed extensions
php glueful extensions:list

# Show detailed extension information
php glueful extensions:info <extension-name>

# Install an extension
php glueful extensions:install <extension-name>

# Update an extension
php glueful extensions:update <extension-name>

# Enable/disable extensions
php glueful extensions:enable <extension-name>
php glueful extensions:disable <extension-name>

# Remove an extension
php glueful extensions:remove <extension-name>

# Validate extension structure
php glueful extensions:validate <extension-name>

# Publish extension assets
php glueful extensions:publish <extension-name>
```

## Scheduler Commands

Task scheduling and background job management.

```bash
# Run scheduler
php glueful scheduler run

# List scheduled tasks
php glueful scheduler list

# Run scheduler in worker mode
php glueful scheduler work

# Worker mode with custom settings
php glueful scheduler work --interval=300 --max-runs=24

# Show next scheduled tasks
php glueful scheduler:upcoming

# Clear failed jobs
php glueful scheduler:clear-failed
```

## Memory Management Commands

Monitor and optimize memory usage.

```bash
# Monitor memory usage
php glueful memory:monitor

# Monitor with custom settings
php glueful memory:monitor --interval=0.5 --threshold=50

# Monitor and log to file
php glueful memory:monitor --log --csv=memory-stats.csv

# Monitor specific command
php glueful memory:monitor php -v

# Show memory statistics
php glueful memory:stats

# Garbage collection
php glueful memory:gc
```

## Notification Commands

Manage notification system and process retries.

```bash
# Process notification retries
php glueful notifications:retry

# Clear failed notifications
php glueful notifications:clear-failed

# Show notification statistics
php glueful notifications:stats

# Test notification channels
php glueful notifications:test <channel>
```

## Logging Commands

Log management and analysis tools.

```bash
# View recent logs
php glueful logs:view

# View specific log file
php glueful logs:view --file=error --date=2025-05-07

# Clear all logs
php glueful logs:clear

# Show log statistics
php glueful logs:stats

# Tail logs in real-time
php glueful logs:tail

# Archive old logs
php glueful logs:archive
```

## Documentation Commands

Generate and manage API documentation.

```bash
# Generate API documentation
php glueful docs:generate

# Export API documentation
php glueful docs:export --format=html --output=./public/docs

# Generate OpenAPI specification
php glueful docs:openapi

# Generate Postman collection
php glueful docs:postman
```

## Security Commands

Security scanning and compliance tools.

```bash
# Run security scan
php glueful security:scan

# Check for vulnerabilities
php glueful security:audit

# Generate security report
php glueful security:report

# Update security definitions
php glueful security:update-definitions
```

## Command Categories

### Core Commands

- `help` - Show available commands and detailed help
- `install` - Install and bootstrap Glueful framework
- `serve` - Start development server
- `system:check` - Validate system requirements

### Database Management

- `db:migrate`, `db:status`, `db:reset` - Migration management
- `db:schema-export`, `db:schema-import` - Schema operations
- `query:profile`, `query:analyze` - Performance analysis

### Cache Operations

- `cache` (clear, status, get, set, delete) - Cache management
- `edge:purge` - Edge cache operations
- `cache:warmup` - Cache preloading

### Extension Management

- `extensions:list`, `extensions:install` - Extension lifecycle
- `extensions:enable`, `extensions:disable` - Extension control
- `extensions:validate` - Extension validation

### System Administration

- `scheduler` (run, list, work) - Task scheduling
- `memory:monitor` - Memory usage monitoring
- `logs:view`, `logs:clear` - Log management
- `notifications:retry` - Notification processing

## Command Options & Flags

Most commands support common options:

```bash
# Get help for any command
php glueful help <command>

# Verbose output (when supported)
php glueful <command> --verbose

# Quiet mode (when supported)
php glueful <command> --quiet

# Force execution (when supported)
php glueful <command> --force

# Dry run mode (when supported)
php glueful <command> --dry-run
```

## Environment-Specific Commands

Some commands behave differently based on environment:

```bash
# Production environment checks
php glueful system:check --production
php glueful config:production

# Development helpers
php glueful serve --port=8080
php glueful memory:monitor --interval=1
```

## Creating Custom Commands

You can extend Glueful's CLI functionality by creating your own custom commands.

### 1. Create Command Class

Create a new command class that extends `Glueful\Console\Command`:

```php
<?php

namespace App\Commands;

use Glueful\Console\Command;

class MyCustomCommand extends Command
{
    /**
     * Get Command Name
     */
    public function getName(): string
    {
        return 'my:command';
    }

    /**
     * Get Command Description
     */
    public function getDescription(): string
    {
        return 'Description of my custom command';
    }

    /**
     * Get detailed help text
     */
    public function getHelp(): string
    {
        return <<<HELP
Custom Command Help

Usage:
  php glueful my:command [options] [arguments]

Options:
  --option1    Description of option 1
  --option2    Description of option 2

Examples:
  php glueful my:command
  php glueful my:command --option1=value
HELP;
    }

    /**
     * Execute the command
     */
    public function execute(array $args = []): int
    {
        $this->info('Executing my custom command...');

        // Parse arguments and options
        $options = $this->parseOptions($args);

        // Your command logic here
        try {
            // Implementation
            $this->success('Command completed successfully');
            return Command::SUCCESS;
        } catch (\Exception $e) {
            $this->error('Command failed: ' . $e->getMessage());
            return Command::FAILURE;
        }
    }

    /**
     * Parse command options
     */
    private function parseOptions(array $args): array
    {
        $options = [];
        foreach ($args as $arg) {
            if (strpos($arg, '--') === 0) {
                $parts = explode('=', substr($arg, 2), 2);
                $options[$parts[0]] = $parts[1] ?? true;
            }
        }
        return $options;
    }
}
```

### 2. Register Command

Register your command in the console kernel:

```php
// In your bootstrap file or service provider
use Glueful\Console\Kernel;

$kernel = new Kernel();
$kernel->registerCommand(\App\Commands\MyCustomCommand::class);
```

### 3. Advanced Command Features

```php
class AdvancedCommand extends Command
{
    public function execute(array $args = []): int
    {
        // Input validation
        if (empty($args)) {
            $this->error('Missing required argument');
            return Command::INVALID;
        }

        // Progress indication
        $this->info('Starting operation...');

        // Table output
        $this->table(
            ['ID', 'Name', 'Status'],
            [
                [1, 'Item 1', 'Active'],
                [2, 'Item 2', 'Inactive']
            ]
        );

        // Colored output
        $this->success('Operation completed');
        $this->warning('Warning message');
        $this->error('Error message');

        return Command::SUCCESS;
    }
}
```

## Console Output Formatting

The Command base class provides comprehensive formatting options:

### Basic Output Methods

```php
// Display messages with different severity levels
$this->info('Information message');
$this->success('Success message in green');
$this->warning('Warning message in yellow');
$this->error('Error message in red');
$this->line('Plain text line');
```

### Advanced Formatting

```php
// Display tables
$this->table(
    ['Column 1', 'Column 2', 'Column 3'],
    [
        ['Row 1 Col 1', 'Row 1 Col 2', 'Row 1 Col 3'],
        ['Row 2 Col 1', 'Row 2 Col 2', 'Row 2 Col 3']
    ]
);

// Progress indicators
$this->info('Processing items...');
for ($i = 0; $i < 100; $i++) {
    // Show progress
    $this->line("Progress: " . ($i + 1) . "/100");
}
```

### Color and Styling

```php
// Manual color control
echo "\033[32mGreen text\033[0m\n";
echo "\033[33mYellow text\033[0m\n";
echo "\033[31mRed text\033[0m\n";
echo "\033[36mCyan text\033[0m\n";
```

## Command Exit Codes

Glueful follows standard Unix exit codes for command results:

- `Command::SUCCESS (0)` - Command executed successfully
- `Command::FAILURE (1)` - General failure during command execution
- `Command::INVALID (2)` - Invalid command usage or input

Example usage in commands:

```php
public function execute(array $args = []): int
{
    // Validate input
    if (!$this->validateInput($args)) {
        $this->error('Invalid input provided');
        return Command::INVALID;
    }

    try {
        // Execute command logic
        $this->performOperation();
        $this->success('Operation completed');
        return Command::SUCCESS;
    } catch (\Exception $e) {
        $this->error('Operation failed: ' . $e->getMessage());
        return Command::FAILURE;
    }
}
```

## Command Debugging

### Debug Mode

Enable verbose output for troubleshooting:

```bash
# Enable debug output
php glueful <command> --verbose

# Combine with specific commands
php glueful system:check --verbose
php glueful extensions:install MyExtension --verbose
```

### Memory Monitoring

Monitor command memory usage:

```bash
# Monitor memory while running command
php glueful memory:monitor php glueful db:migrate

# Monitor with logging
php glueful memory:monitor --log --csv=migration-memory.csv php glueful db:migrate
```

## Automation & Scripting

### Batch Operations

Create shell scripts for common operations:

```bash
#!/bin/bash
# deploy.sh - Deployment script

echo "Starting deployment..."

# Run database migrations
php glueful db:migrate

# Clear cache
php glueful cache clear

# Warm up cache
php glueful cache:warmup

# Check system health
php glueful system:check --production

echo "Deployment completed"
```

### Scheduler Integration

Use commands with the task scheduler:

```php
// In your scheduler configuration
$scheduler->command('cache:clear')->daily();
$scheduler->command('logs:archive')->weekly();
$scheduler->command('extensions:update')->monthly();
```

## Best Practices

### Command Design

- **Single Responsibility**: Each command should have one clear purpose
- **Descriptive Names**: Use clear, consistent naming (e.g., `namespace:action`)
- **Comprehensive Help**: Provide detailed help text with examples
- **Error Handling**: Always handle exceptions and return appropriate exit codes

### Performance

- **Memory Awareness**: Monitor memory usage for long-running commands
- **Progress Feedback**: Show progress for operations that take time
- **Batch Processing**: Process large datasets in chunks

### Security

- **Input Validation**: Always validate and sanitize command arguments
- **Permission Checks**: Verify user permissions for sensitive operations
- **Safe Defaults**: Use safe defaults and require explicit flags for destructive operations

### Development Workflow

- **Use Help System**: Explore `php glueful help` to discover available functionality
- **Test Commands**: Test commands in development before production use
- **Script Common Tasks**: Create scripts for frequently used command sequences
- **Monitor Resources**: Use memory monitoring for resource-intensive operations

### Production Considerations

- **Dry Run Mode**: Use `--dry-run` flags when available to preview changes
- **Backup First**: Always backup data before running destructive commands
- **Schedule Maintenance**: Use scheduler for routine maintenance tasks
- **Log Operations**: Enable logging for audit trails in production
