---
title: Schema
description: Working with database schema in Glueful
navigation:
  icon: i-lucide-database
---

Glueful provides powerful schema management tools through the SchemaManager classes, which support multiple database engines including MySQL, PostgreSQL, and SQLite.

## SchemaManager Overview

The schema manager allows you to create, modify, and manage database structures using a consistent API regardless of the underlying database engine.

```php
use Glueful\Database\Connection;

// Get the appropriate schema manager for your database
$connection = new Connection();
$schema = $connection->getSchemaManager();
```

## Creating Tables

Create tables with columns, indexes, and foreign keys:

```php
$schema->createTable('users', [
    'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT',
    'uuid' => 'CHAR(12) NOT NULL',
    'username' => 'VARCHAR(255) NOT NULL',
    'email' => 'VARCHAR(255) NOT NULL',
    'password' => 'VARCHAR(100) NOT NULL',
    'status' => "VARCHAR(20) DEFAULT 'active'",
    'created_at' => 'TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP',
    'updated_at' => 'TIMESTAMP NULL ON UPDATE CURRENT_TIMESTAMP'
]);
```

## Adding Indexes

Add various types of indexes to optimize queries:

```php
// Add indexes to an existing or newly created table
$schema->createTable('products', [
    'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT',
    'name' => 'VARCHAR(255) NOT NULL',
    'price' => 'DECIMAL(10,2) NOT NULL',
    'category_id' => 'BIGINT NOT NULL'
])->addIndex([
    // Single column unique index
    ['type' => 'UNIQUE', 'column' => 'name', 'table' => 'products'],

    // Regular index
    ['type' => 'INDEX', 'column' => 'category_id', 'table' => 'products'],

    // Multi-column index
    ['type' => 'INDEX', 'column' => ['name', 'price'], 'table' => 'products']
]);
```

## Foreign Keys

Establish relationships between tables:

```php
$schema->createTable('orders', [
    'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT',
    'user_id' => 'BIGINT NOT NULL',
    'total' => 'DECIMAL(10,2) NOT NULL',
    'status' => 'VARCHAR(50) NOT NULL'
])->addForeignKey([
    [
        'column' => 'user_id',
        'references' => 'id',
        'on' => 'users',
        'onDelete' => 'CASCADE',
        'onUpdate' => 'CASCADE'
    ]
]);
```

## Modifying Tables

Modify existing tables by adding, renaming or modifying columns:

```php
// Add a new column to an existing table
$schema->addColumn('users', 'phone_number', 'VARCHAR(20) NULL');

// Modify column
$schema->modifyColumn('users', 'status', "ENUM('active', 'inactive', 'banned') DEFAULT 'active'");

// Rename column
$schema->renameColumn('users', 'phone_number', 'contact_number');
```

## Dropping Tables and Columns

Remove database objects when no longer needed:

```php
// Drop a table
$schema->dropTable('temp_users');

// Drop a column
$schema->dropColumn('users', 'unused_column');

// Drop an index
$schema->dropIndex('users', 'index_name');
```

## Database-Specific Features

Each SchemaManager implementation provides engine-specific features:

### MySQL Features

```php
// MySQL-specific table options
$schema->createTable('large_data', [
    'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT',
    'data' => 'LONGBLOB NOT NULL'
], [
    'engine' => 'InnoDB',
    'charset' => 'utf8mb4',
    'collation' => 'utf8mb4_unicode_ci',
    'comment' => 'Stores large binary data'
]);

// Full-text search index
$schema->addIndex([
    'type' => 'FULLTEXT',
    'column' => ['title', 'content'],
    'table' => 'articles'
]);
```

### PostgreSQL Features

```php
// PostgreSQL-specific table options
$schema->createTable('geo_data', [
    'id' => 'BIGSERIAL PRIMARY KEY',
    'point' => 'GEOMETRY(POINT, 4326) NOT NULL'
]);

// GIN index for fast full-text search
$schema->addIndex([
    'type' => 'GIN',
    'column' => 'search_vector',
    'table' => 'documents'
]);
```

### SQLite Features

```php
// SQLite WITHOUT ROWID optimization
$schema->createTable('lookup', [
    'id' => 'INTEGER PRIMARY KEY',
    'code' => 'TEXT NOT NULL',
    'value' => 'TEXT NOT NULL'
], [
    'without_rowid' => true
]);
```

## Retrieving Schema Information

Inspect database schema:

```php
// Get table structure
$tableInfo = $schema->getTableStructure('users');

// Get table size
$size = $schema->getTableSize('large_data');

// Get row count
$count = $schema->getTableRowCount('users');
```

## Performance Considerations

- Use appropriate index types for your query patterns
- Be careful with adding too many indexes as they slow down write operations
- Consider using foreign key constraints only for critical relationships
- For large tables, always use indexed columns in WHERE clauses
- Be mindful of schema changes on large tables as they can lock tables during alteration

## Transaction Support

Wrap schema operations in transactions for atomic changes:

```php
// Begin transaction
$connection->getPDO()->beginTransaction();

try {
    // Create table
    $schema->createTable('transactions', [
        'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT',
        'amount' => 'DECIMAL(10,2) NOT NULL',
        'description' => 'TEXT NULL',
        'created_at' => 'TIMESTAMP DEFAULT CURRENT_TIMESTAMP'
    ]);

    // Add indexes
    $schema->addIndex([
        ['type' => 'INDEX', 'column' => 'created_at', 'table' => 'transactions']
    ]);

    // Commit all changes
    $connection->getPDO()->commit();
} catch (Exception $e) {
    // Rollback on error
    $connection->getPDO()->rollBack();
    throw $e;
}
```

## Multi-Database Support

The SchemaManager automatically adapts SQL syntax for different engines:

```php
// This code works across MySQL, PostgreSQL, and SQLite
$schema->createTable('portable_table', [
    'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT', // Translated appropriately for each engine
    'name' => 'VARCHAR(255) NOT NULL',
    'created_at' => 'TIMESTAMP DEFAULT CURRENT_TIMESTAMP'
]);
```

## Real-World Examples

### User Profile System

```php
// Create a complete user profile system
$schema->createTable('profiles', [
    'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT',
    'uuid' => 'CHAR(12) NOT NULL',
    'user_uuid' => 'CHAR(12) NOT NULL',
    'first_name' => 'VARCHAR(100) DEFAULT NULL',
    'last_name' => 'VARCHAR(100) DEFAULT NULL',
    'photo_uuid' => 'CHAR(12) DEFAULT NULL',
    'photo_url' => 'VARCHAR(255) DEFAULT NULL',
    'status' => "VARCHAR(20) NOT NULL CHECK (status IN ('active', 'inactive', 'deleted'))",
    'created_at' => 'TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP',
    'updated_at' => 'TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP'
])->addIndex([
    ['type' => 'UNIQUE', 'column' => 'uuid'],
    ['type' => 'INDEX', 'column' => 'user_uuid'],
    ['type' => 'INDEX', 'column' => 'photo_uuid']
])->addForeignKey([
    [
        'column' => 'user_uuid',
        'references' => 'uuid',
        'on' => 'users',
        'onDelete' => 'RESTRICT'
    ],
    [
        'column' => 'photo_uuid',
        'references' => 'uuid',
        'on' => 'blobs',
        'onDelete' => 'SET NULL'
    ]
]);
```

### Notification System

```php
// Create a notification system
$schema->createTable('notifications', [
    'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT',
    'uuid' => 'CHAR(12) NOT NULL',
    'user_uuid' => 'CHAR(12) NOT NULL',
    'type' => 'VARCHAR(50) NOT NULL',
    'title' => 'VARCHAR(255) NOT NULL',
    'content' => 'TEXT NOT NULL',
    'data' => 'JSON NULL',
    'read_at' => 'TIMESTAMP NULL',
    'status' => 'VARCHAR(20) NOT NULL',
    'created_at' => 'TIMESTAMP DEFAULT CURRENT_TIMESTAMP'
])->addIndex([
    ['type' => 'UNIQUE', 'column' => 'uuid'],
    ['type' => 'INDEX', 'column' => 'user_uuid'],
    ['type' => 'INDEX', 'column' => ['user_uuid', 'read_at'], 'name' => 'idx_notifications_user_read'],
    ['type' => 'INDEX', 'column' => ['type', 'status'], 'name' => 'idx_notifications_type_status']
])->addForeignKey([
    [
        'column' => 'user_uuid',
        'references' => 'uuid',
        'on' => 'users',
        'onDelete' => 'CASCADE'
    ]
]);
```

## Integration with Migrations

Glueful's SchemaManager is designed to work seamlessly with the built-in migration system:

```php
<?php
// In a migration file

use Glueful\Database\Migrations\MigrationInterface;
use Glueful\Database\Schema\SchemaManager;

class CreateSocialAccountsTable implements MigrationInterface
{
    public function up(SchemaManager $schema): void
    {
        $schema->createTable('social_accounts', [
            'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT',
            'uuid' => 'CHAR(12) NOT NULL',
            'user_uuid' => 'CHAR(12) NOT NULL', 
            'provider' => 'VARCHAR(50) NOT NULL',
            'social_id' => 'VARCHAR(255) NOT NULL',
            'profile_data' => 'TEXT NULL',
            'created_at' => 'TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP',
            'updated_at' => 'TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP'
        ])->addIndex([
            ['type' => 'UNIQUE', 'column' => 'uuid'],
            ['type' => 'INDEX', 'column' => 'user_uuid'],
            ['type' => 'UNIQUE', 'column' => ['provider', 'social_id']]
        ])->addForeignKey([
            [
                'column' => 'user_uuid',
                'references' => 'uuid',
                'on' => 'users',
                'onDelete' => 'CASCADE'
            ]
        ]);
    }
    
    public function down(SchemaManager $schema): void
    {
        $schema->dropTable('social_accounts');
    }
    
    public function getDescription(): string
    {
        return "Create social_accounts table for social login integration";
    }
}
```

## Indexing Strategy Best Practices

Glueful follows these best practices for indexing:

1. **Primary keys** are automatically indexed
2. **Foreign keys** should be indexed to improve join performance
3. **Unique constraints** are enforced with unique indexes
4. **Frequently queried fields** should have appropriate indexes
5. **Multi-column indexes** for queries that filter on multiple columns

Index naming follows a consistent pattern:
- `idx_[table]_[column1]_[column2]` for multi-column indexes
- `idx_[table]_[column]` for single-column indexes

This strategy ensures optimal query performance while maintaining clean and readable schema definitions.

## Advanced Schema Management Features

Glueful's SchemaManager includes enterprise-grade features for complex schema operations:

### Schema Validation and Preview

```php
use Glueful\Database\Schema\SchemaValidator;
use Glueful\Database\Schema\SchemaPreview;

// Validate schema before applying changes
$validator = new SchemaValidator($schema);

$validationResult = $validator->validateSchema([
    'table' => 'users',
    'columns' => [
        'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT',
        'email' => 'VARCHAR(255) NOT NULL UNIQUE'
    ],
    'indexes' => [
        ['type' => 'UNIQUE', 'column' => 'email']
    ]
]);

if (!$validationResult['valid']) {
    foreach ($validationResult['errors'] as $error) {
        echo "Validation Error: {$error['message']}\n";
    }
}

// Preview schema changes before execution
$preview = new SchemaPreview($schema);
$changes = $preview->previewChanges([
    'action' => 'create_table',
    'table' => 'new_users',
    'definition' => [
        'id' => 'BIGINT PRIMARY KEY AUTO_INCREMENT',
        'username' => 'VARCHAR(255) NOT NULL'
    ]
]);

echo "SQL to be executed:\n";
foreach ($changes['sql'] as $statement) {
    echo "{$statement}\n";
}
echo "Estimated execution time: {$changes['estimated_time']}ms\n";
```

### Schema Export and Import

```php
use Glueful\Database\Schema\SchemaExporter;
use Glueful\Database\Schema\SchemaImporter;

// Export schema to different formats
$exporter = new SchemaExporter($schema);

// Export to JSON
$jsonSchema = $exporter->exportToJson(['users', 'profiles', 'notifications']);
file_put_contents('schema.json', $jsonSchema);

// Export to SQL
$sqlSchema = $exporter->exportToSql(['users', 'profiles'], [
    'include_data' => false,
    'include_indexes' => true,
    'include_foreign_keys' => true,
    'target_engine' => 'mysql'
]);
file_put_contents('schema.sql', $sqlSchema);

// Export to YAML for documentation
$yamlSchema = $exporter->exportToYaml(['users'], [
    'include_comments' => true,
    'include_constraints' => true
]);

// Export to PHP array
$phpSchema = $exporter->exportToPhp(['users'], [
    'include_migration_format' => true
]);

// Import schema from file
$importer = new SchemaImporter($schema);

// Import from JSON
$importer->importFromJson('schema.json', [
    'validate_before_import' => true,
    'backup_existing' => true,
    'dry_run' => false
]);

// Import with conflict resolution
$importer->importFromSql('external_schema.sql', [
    'conflict_resolution' => 'merge', // merge, replace, skip
    'table_prefix' => 'imported_',
    'rename_conflicts' => true
]);
```

### Schema Comparison and Diff

```php
use Glueful\Database\Schema\SchemaDiff;

$diff = new SchemaDiff($schema);

// Compare current schema with target schema
$differences = $diff->compareSchemas('current_db', 'target_db');

echo "Schema Differences Found:\n";
foreach ($differences as $diff) {
    echo "Type: {$diff['type']}\n";
    echo "Object: {$diff['object_name']}\n";
    echo "Action: {$diff['action']}\n";
    echo "Details: {$diff['details']}\n\n";
}

// Generate migration script from differences
$migrationSql = $diff->generateMigrationScript($differences, [
    'target_engine' => 'mysql',
    'preserve_data' => true,
    'add_rollback_statements' => true
]);

// Compare schema with file
$fileComparison = $diff->compareWithFile('expected_schema.json', [
    'ignore_auto_increment' => true,
    'ignore_timestamps' => false,
    'ignore_comments' => true
]);
```

### Advanced Index Management

```php
use Glueful\Database\Schema\IndexAnalyzer;
use Glueful\Database\Schema\IndexOptimizer;

// Analyze index performance and usage
$indexAnalyzer = new IndexAnalyzer($schema);

$analysis = $indexAnalyzer->analyzeTable('users', [
    'include_usage_stats' => true,
    'include_performance_impact' => true,
    'analyze_query_patterns' => true
]);

foreach ($analysis['recommendations'] as $recommendation) {
    echo "Index Recommendation: {$recommendation['message']}\n";
    echo "Impact: {$recommendation['impact']}\n";
    echo "SQL: {$recommendation['sql']}\n\n";
}

// Optimize indexes automatically
$optimizer = new IndexOptimizer($schema);

$optimizations = $optimizer->optimizeTable('orders', [
    'remove_unused_indexes' => true,
    'add_missing_indexes' => true,
    'optimize_composite_indexes' => true,
    'dry_run' => false
]);

// Create specialized indexes
$schema->addAdvancedIndex('users', [
    'type' => 'COMPOSITE',
    'columns' => ['status', 'created_at'],
    'name' => 'idx_users_status_created',
    'order' => ['ASC', 'DESC'],
    'where' => "status IN ('active', 'pending')", // Partial index
    'include' => ['email', 'username'] // Covering index
]);

// Create functional indexes (PostgreSQL)
if ($schema->getEngine() === 'pgsql') {
    $schema->addAdvancedIndex('users', [
        'type' => 'FUNCTIONAL',
        'expression' => 'LOWER(email)',
        'name' => 'idx_users_email_lower'
    ]);
}
```

### Schema Versioning and History

```php
use Glueful\Database\Schema\SchemaVersioning;

$versioning = new SchemaVersioning($schema);

// Create schema snapshot
$snapshot = $versioning->createSnapshot('before_major_update', [
    'include_data' => false,
    'include_procedures' => true,
    'include_views' => true,
    'compress' => true
]);

// List all schema versions
$versions = $versioning->listVersions();
foreach ($versions as $version) {
    echo "Version: {$version['name']}\n";
    echo "Created: {$version['created_at']}\n";
    echo "Size: {$version['size']}\n";
    echo "Tables: " . implode(', ', $version['tables']) . "\n\n";
}

// Restore from snapshot
$versioning->restoreFromSnapshot('before_major_update', [
    'backup_current' => true,
    'verify_integrity' => true,
    'tables_only' => ['users', 'profiles'] // Selective restore
]);

// Compare versions
$versionDiff = $versioning->compareVersions('v1.0', 'v2.0');
```

### Constraint Management

```php
use Glueful\Database\Schema\ConstraintManager;

$constraints = new ConstraintManager($schema);

// Add complex constraints
$constraints->addCheckConstraint('users', 'chk_email_format', 
    "email ~ '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$'"
);

$constraints->addCheckConstraint('orders', 'chk_positive_total',
    "total > 0"
);

// Add exclusion constraints (PostgreSQL)
if ($schema->getEngine() === 'pgsql') {
    $constraints->addExclusionConstraint('bookings', 
        'excl_time_conflict',
        ['room_id WITH =', 'time_range WITH &&']
    );
}

// Manage foreign key constraints with advanced options
$constraints->addForeignKeyWithOptions('order_items', [
    'column' => 'order_id',
    'references' => 'id',
    'on' => 'orders',
    'onDelete' => 'CASCADE',
    'onUpdate' => 'RESTRICT',
    'deferrable' => true, // PostgreSQL
    'initially_deferred' => true // PostgreSQL
]);

// Validate all constraints
$validationResults = $constraints->validateAllConstraints();
foreach ($validationResults as $result) {
    if (!$result['valid']) {
        echo "Constraint violation: {$result['constraint']}\n";
        echo "Details: {$result['details']}\n";
    }
}
```

### Table Partitioning

```php
use Glueful\Database\Schema\TablePartitioning;

$partitioning = new TablePartitioning($schema);

// Create partitioned table (PostgreSQL)
if ($schema->getEngine() === 'pgsql') {
    $partitioning->createPartitionedTable('events', [
        'columns' => [
            'id' => 'BIGSERIAL',
            'event_date' => 'DATE NOT NULL',
            'event_type' => 'VARCHAR(50) NOT NULL',
            'data' => 'JSONB'
        ],
        'partition_by' => 'RANGE (event_date)',
        'partitions' => [
            [
                'name' => 'events_2024',
                'for_values' => "FROM ('2024-01-01') TO ('2025-01-01')"
            ],
            [
                'name' => 'events_2025',
                'for_values' => "FROM ('2025-01-01') TO ('2026-01-01')"
            ]
        ]
    ]);
}

// Create sharded tables (MySQL)
if ($schema->getEngine() === 'mysql') {
    $partitioning->createShardedTable('user_activities', [
        'columns' => [
            'id' => 'BIGINT AUTO_INCREMENT',
            'user_id' => 'BIGINT NOT NULL',
            'activity_date' => 'DATE NOT NULL',
            'data' => 'JSON'
        ],
        'partition_by' => 'HASH(user_id)',
        'partition_count' => 8
    ]);
}
```

### Schema Security and Permissions

```php
use Glueful\Database\Schema\SchemaSecurityManager;

$security = new SchemaSecurityManager($schema);

// Set table-level permissions
$security->setTablePermissions('sensitive_data', [
    'app_readonly' => ['SELECT'],
    'app_user' => ['SELECT', 'INSERT', 'UPDATE'],
    'app_admin' => ['SELECT', 'INSERT', 'UPDATE', 'DELETE'],
]);

// Set column-level permissions
$security->setColumnPermissions('users', [
    'password' => [
        'app_readonly' => [], // No access
        'app_user' => ['UPDATE'], // Can only update own password
        'app_admin' => ['SELECT', 'UPDATE']
    ],
    'email' => [
        'app_readonly' => ['SELECT'],
        'app_user' => ['SELECT', 'UPDATE'],
        'app_admin' => ['SELECT', 'UPDATE', 'DELETE']
    ]
]);

// Enable row-level security (PostgreSQL)
if ($schema->getEngine() === 'pgsql') {
    $security->enableRowLevelSecurity('user_data');
    $security->createPolicy('user_data', 'user_isolation_policy',
        'FOR ALL TO app_user USING (user_id = current_user_id())'
    );
}

// Audit schema access
$auditLog = $security->getSchemaAccessLog([
    'start_date' => '2025-01-01',
    'end_date' => '2025-01-31',
    'operations' => ['CREATE', 'ALTER', 'DROP']
]);
```

### Schema Performance Optimization

```php
use Glueful\Database\Schema\SchemaOptimizer;

$optimizer = new SchemaOptimizer($schema);

// Analyze schema performance
$analysis = $optimizer->analyzeSchemaPerformance([
    'include_table_sizes' => true,
    'include_index_usage' => true,
    'include_query_patterns' => true,
    'analyze_foreign_keys' => true
]);

// Optimize schema based on usage patterns
$optimizations = $optimizer->generateOptimizations([
    'optimize_data_types' => true,
    'suggest_partitioning' => true,
    'optimize_indexes' => true,
    'suggest_archiving' => true
]);

foreach ($optimizations as $optimization) {
    echo "Optimization: {$optimization['type']}\n";
    echo "Table: {$optimization['table']}\n";
    echo "Benefit: {$optimization['estimated_benefit']}\n";
    echo "SQL: {$optimization['sql']}\n\n";
}

// Auto-optimize schema
$optimizer->autoOptimize([
    'max_downtime' => 300, // 5 minutes max
    'backup_before_changes' => true,
    'verify_after_changes' => true,
    'rollback_on_failure' => true
]);
```

### CLI Schema Management Commands

Glueful provides comprehensive CLI commands for schema management:

```bash
# Schema analysis and validation
php glueful schema:analyze --table=users --detailed
php glueful schema:validate --check-constraints --check-foreign-keys
php glueful schema:compare --source=dev --target=staging

# Schema export and import
php glueful schema:export --format=json --tables=users,profiles --output=schema.json
php glueful schema:import --file=schema.json --validate --backup
php glueful schema:dump --include-data --compress --encrypt

# Index management
php glueful schema:index:analyze --table=users --suggest-optimizations
php glueful schema:index:optimize --table=orders --remove-unused
php glueful schema:index:rebuild --table=large_table --online

# Schema versioning
php glueful schema:snapshot --name=pre_deployment --compress
php glueful schema:restore --snapshot=pre_deployment --verify
php glueful schema:diff --version1=v1.0 --version2=v2.0

# Performance optimization
php glueful schema:optimize --all-tables --analyze-queries
php glueful schema:performance --report --period="last 30 days"
```

These advanced schema management features provide enterprise-level capabilities for maintaining complex database schemas while ensuring performance, security, and reliability.
