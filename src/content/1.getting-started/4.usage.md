---
title: Basic Usage
description: Getting started with using the Glueful framework
navigation:
  icon: i-lucide-code
---

This guide covers essential usage patterns for developing modern APIs with Glueful, including the DI container, session management, queue system, and enterprise features.

## Project Structure

A modern Glueful project includes these enterprise-ready components:

```
glueful/
├── api/                           # Core framework architecture
│   ├── Attributes/                # PHP attributes for metadata
│   ├── Auth/                      # JWT authentication & TokenStorageService
│   │   ├── Interfaces/            # Authentication contracts
│   │   ├── SessionAnalytics.php   # Session tracking & analytics
│   │   ├── TokenStorageService.php # Dual-layer session management
│   │   └── ...                    # JWT, LDAP, SAML providers
│   ├── Cache/                     # Multi-driver caching with edge support
│   │   ├── CDN/                   # CDN adapter implementations (AbstractCDNAdapter, CDNAdapterInterface)
│   │   ├── Health/                # Cache health monitoring (CircuitBreaker, FailoverManager, HealthMonitoring)
│   │   ├── Nodes/                 # Cache node management (CacheNode, FileNode, RedisNode, MemcachedNode)
│   │   ├── Replication/           # Cache replication strategies (ConsistentHashing, PrimaryReplica, FullReplication)
│   │   └── drivers/               # Redis, Memcached, File drivers (RedisCacheDriver, MemcachedCacheDriver)
│   ├── Console/                   # CLI command framework
│   │   ├── Commands/              # All CLI commands
│   │   │   ├── Notifications/     # Notification management commands
│   │   │   └── Queue/             # Queue management commands
│   │   └── Templates/             # Code generation templates
│   ├── Constants/                 # Application constants
│   ├── Controllers/               # API controllers with trait system
│   │   └── Traits/                # Reusable controller functionality
│   ├── Cron/                      # Scheduled task handlers
│   ├── DI/                        # Dependency injection container
│   │   ├── Exceptions/            # DI container exceptions
│   │   ├── Interfaces/            # DI contracts
│   │   ├── Providers/             # Core service providers
│   │   └── ServiceProviders/      # Additional service providers
│   ├── DTOs/                      # Data Transfer Objects
│   ├── Database/                  # Database layer with connection pooling
│   │   ├── Attributes/            # Database attributes (CacheResult)
│   │   ├── Driver/                # Database driver implementations (MySQL, PostgreSQL, SQLite)
│   │   ├── Exceptions/            # Database exceptions (ConnectionPoolException)
│   │   ├── Execution/             # Query execution layer (ParameterBinder, QueryExecutor, ResultProcessor)
│   │   │   └── Interfaces/        # Query execution contracts
│   │   ├── Features/              # Database features (Pagination, QueryValidator, SoftDeleteHandler)
│   │   │   └── Interfaces/        # Feature contracts
│   │   ├── Migrations/            # Migration framework (MigrationManager, MigrationInterface)
│   │   ├── Query/                 # Query builders (Select, Insert, Update, Delete, JoinClause, WhereClause)
│   │   │   └── Interfaces/        # Query builder contracts
│   │   ├── Schema/                # Schema management with builders, DTOs, and SQL generators
│   │   │   ├── Builders/          # Schema builders (SchemaBuilder, TableBuilder, ColumnBuilder)
│   │   │   ├── DTOs/              # Schema data transfer objects
│   │   │   ├── Generators/        # SQL generators for different databases
│   │   │   └── Interfaces/        # Schema contracts
│   │   ├── Tools/                 # Query analysis & profiling (ExecutionPlanAnalyzer, QueryProfiler)
│   │   └── Transaction/           # Transaction management (TransactionManager, SavepointManager)
│   │       └── Interfaces/        # Transaction contracts
│   ├── Events/                    # Event system
│   ├── Exceptions/                # Comprehensive exception handling
│   ├── Helpers/                   # Utility classes & traits
│   ├── Http/                      # PSR-15 middleware & routing
│   │   ├── Middleware/            # Middleware implementations
│   │   ├── Router.php             # Route management
│   │   └── SecureErrorResponse.php # Secure error handling
│   ├── Interfaces/                # Framework contracts
│   │   └── Permission/            # Permission system interfaces
│   ├── Logging/                   # Advanced logging system
│   ├── Models/                    # Data models
│   ├── Notifications/             # Multi-channel notification system
│   │   ├── Contracts/             # Notification contracts
│   │   ├── Events/                # Notification events
│   │   ├── Models/                # Notification models
│   │   ├── Services/              # Notification services
│   │   ├── Templates/             # Template management
│   │   └── Utils/                 # Notification utilities
│   ├── Performance/               # Memory management & optimization
│   ├── Permissions/               # RBAC authorization system
│   │   ├── Exceptions/            # Permission exceptions
│   │   └── Helpers/               # Permission utilities
│   ├── Queue/                     # Background job processing
│   │   ├── Config/                # Queue configuration (ConfigManager, ConfigValidator)
│   │   ├── Contracts/             # Queue contracts (JobInterface, QueueDriverInterface)
│   │   ├── Discovery/             # Driver discovery (DriverDiscovery)
│   │   ├── Drivers/               # Queue driver implementations (DatabaseQueue, RedisQueue)
│   │   ├── Events/                # Queue events (EventDispatcher)
│   │   ├── Exceptions/            # Queue exceptions (QueueException, DriverNotFoundException)
│   │   ├── Failed/                # Failed job handling (FailedJobProvider)
│   │   ├── Jobs/                  # Job implementations (DatabaseJob, RedisJob, SendNotification)
│   │   ├── Monitoring/            # Worker monitoring (WorkerMonitor)
│   │   ├── Plugins/               # Queue plugins (PluginManager)
│   │   ├── Process/               # Process management (AutoScaler, ProcessManager, WorkerProcess)
│   │   └── Registry/              # Driver registry (DriverRegistry)
│   ├── Repository/                # Data access layer
│   │   ├── Concerns/              # Repository concerns
│   │   ├── Interfaces/            # Repository contracts
│   │   └── Traits/                # Repository traits
│   ├── Scheduler/                 # Task scheduling
│   ├── Security/                  # CSRF, rate limiting, lockdown
│   ├── Services/                  # Business logic services
│   │   └── Archive/               # Data retention & compliance
│   ├── Uploader/                  # File upload handling
│   │   └── Storage/               # Storage implementations
│   ├── Validation/                # Input validation & sanitization
│   │   └── Attributes/            # Validation attributes
│   ├── api-json-definitions/      # API schema definitions
│   ├── bootstrap.php              # Application bootstrap
│   ├── helpers.php                # Global helper functions
│   └── version.php                # Version information
├── config/                        # Smart environment-aware configs
│   ├── app.php                    # Application settings
│   ├── archive.php                # Data retention policies
│   ├── cache.php                  # Cache drivers
│   ├── database.php               # Connection pooling
│   ├── lockdown.php               # Emergency security
│   ├── middleware.php             # Middleware configuration
│   ├── queue.php                  # Job queue settings
│   ├── resource.php               # Resource configuration
│   ├── schedule.php               # Cron job management
│   ├── security.php               # Security policies
│   ├── services.php               # External services
│   └── session.php                # JWT & session config
├── database/                      # Schema management
│   └── migrations/                # Versioned database changes
├── docs/                          # Comprehensive documentation
│   ├── api-doc-json-definitions/  # API documentation schemas
│   └── features/                  # Feature documentation
├── extensions/                    # Extension System v2.0
│   ├── Admin/                     # Admin interface extension with SPA
│   ├── BeanstalkdQueue/           # Beanstalkd queue driver extension
│   ├── EmailNotification/         # Email notification channel extension
│   ├── RBAC/                      # Role-based access control extension
│   ├── SocialLogin/               # Social authentication extension
│   ├── CREATING_EXTENSIONS.md     # Extension development guide
│   ├── README.md                  # Extensions overview
│   └── extensions.json            # Extension registry
├── routes/                        # API route definitions
│   ├── auth.php                   # Authentication routes
│   ├── extensions.php             # Extension routes
│   ├── files.php                  # File management routes
│   ├── health.php                 # Health check routes
│   ├── notifications.php          # Notification routes
│   ├── public.php                 # Public routes
│   └── resource.php               # Resource routes
├── storage/                       # Application data
│   ├── archives/                  # Archived data
│   ├── backups/                   # Database backups
│   ├── cache/                     # Cache storage
│   ├── cdn/                       # CDN assets
│   ├── framework/                 # Framework storage
│   │   └── locks/                 # Lock files
│   ├── jobs/                      # Job storage
│   ├── logs/                      # Application logs (app, debug, queries, framework)
│   └── vulnerability_scans/       # Security scan results
├── tests/                         # Comprehensive test suites
│   ├── Alternative/               # Alternative test implementations
│   ├── Cache/                     # Cache system tests
│   ├── Extensions/                # Extension tests
│   ├── Fixtures/                  # Test fixtures
│   ├── Helpers/                   # Test helpers
│   ├── Integration/               # Integration tests
│   ├── Mocks/                     # Mock implementations
│   └── Unit/                      # Unit tests
├── tools/                         # Development tools
├── composer.json                  # PHP dependencies
├── phpcs.xml                      # Code style configuration
├── phpstan.neon                   # Static analysis configuration
├── phpunit.xml                    # Test configuration
└── glueful                        # CLI executable
```

## Dependency Injection Container

Glueful's DI container is central to the framework architecture:

### Basic Usage

```php
// Accessing the global container
$container = container();

// Getting services
$userService = $container->get(UserService::class);
$database = $container->get(DatabaseInterface::class);
$cache = $container->get(CacheInterface::class);

// In controllers, services are injected automatically
class UserController extends BaseController
{
    public function __construct(
        private UserService $userService,
        private NotificationService $notificationService
    ) {}
}
```

### Service Providers

Register services using service providers:

```php
// api/DI/ServiceProviders/UserServiceProvider.php
class UserServiceProvider implements ServiceProviderInterface
{
    public function register(): void
    {
        $this->container->singleton(UserService::class, function($container) {
            return new UserService(
                $container->get(UserRepository::class),
                $container->get(CacheInterface::class)
            );
        });
    }
}

// Register in bootstrap.php
$providers = [
    \Glueful\DI\ServiceProviders\UserServiceProvider::class,
];
```

### Container Commands

```bash
# Validate container configuration
php glueful di:container:validate

# Debug container issues
php glueful di:container:debug

# Compile container for production
php glueful di:container:compile
```

## Extension System v2.0

High-performance extension system with 50x faster loading:

```bash
# Create new extension
php glueful extensions:create MyExtension

# List extensions and get info
php glueful extensions:info
php glueful extensions:info MyExtension

# Enable/disable extensions
php glueful extensions:enable MyExtension
php glueful extensions:disable MyExtension

# Validate extension configuration
php glueful extensions:validate MyExtension

# Performance benchmarking
php glueful extensions:benchmark

# Debug extension system
php glueful extensions:debug
```

### Extension Structure

```
extensions/MyExtension/
├── manifest.json         # Extension metadata (v2.0 manifest)
├── composer.json        # Dependencies
├── src/                 # Source code
│   ├── Controllers/     # Extension controllers
│   ├── Services/        # Extension services
│   ├── Models/          # Extension models
│   ├── Repositories/    # Extension repositories
│   └── ServiceProvider.php # DI registration
├── routes.php           # Extension routes
├── config.php           # Extension configuration
├── migrations/          # Database migrations
├── assets/              # Extension assets (icons, etc.)
└── public/              # Public web assets
```

## Modern Routing System

Glueful's routing system supports PSR-15 middleware and OpenAPI documentation:

### Basic Routes with Authentication

```php
<?php
// routes/api.php

use Glueful\Http\Router;
use Glueful\Controllers\UserController;

// Public routes
Router::post('/auth/login', [AuthController::class, 'login']);
Router::post('/auth/register', [AuthController::class, 'register']);

// Protected routes
Router::get('/users', [UserController::class, 'index'], requiresAuth: true);
Router::post('/users', [UserController::class, 'store'], requiresAuth: true);
Router::get('/users/{uuid}', [UserController::class, 'show'], requiresAuth: true);
Router::put('/users/{uuid}', [UserController::class, 'update'], requiresAuth: true);
Router::delete('/users/{uuid}', [UserController::class, 'destroy'], requiresAuth: true);

// Admin-only routes
Router::group('/admin', function() {
    Router::get('/users', [AdminController::class, 'listUsers']);
    Router::post('/users', [AdminController::class, 'createUser']);
}, requiresAuth: true, requiresAdminAuth: true);
```

### OpenAPI Documentation

Use annotations for automatic API documentation:

```php
/**
 * @route GET /users
 * @summary List Users
 * @description Retrieve paginated list of users
 * @tag Users
 * @requiresAuth true
 * @param page query integer false "Page number"
 * @param limit query integer false "Items per page"
 * @response 200 application/json "Users retrieved successfully" {
 *   data:array="Array of user objects",
 *   pagination:object="Pagination metadata"
 * }
 * @response 401 "Unauthorized"
 */
public function index(Request $request): Response
{
    // Controller implementation
}
```

### Route Groups with Middleware

```php
// Routes with rate limiting
Router::group('/api/v1', function() {
    Router::get('/public-data', [PublicController::class, 'getData']);
}, middleware: ['rate-limit:60,1']);

// Routes with custom middleware
Router::group('/sensitive', function() {
    Router::get('/data', [SensitiveController::class, 'getData']);
}, middleware: ['auth', 'permission:admin', 'audit-log']);
```

## Session Management with TokenStorageService

Glueful uses dual-layer session storage (database + cache) for enterprise-grade session management:

### Basic Session Operations

```php
use Glueful\Auth\TokenStorageService;

$tokenStorage = container()->get(TokenStorageService::class);

// Store session with metadata
$sessionData = [
    'uuid' => $userUuid,
    'username' => $username,
    'email' => $email
];
$tokens = [
    'access_token' => $accessToken,
    'refresh_token' => $refreshToken
];
$success = $tokenStorage->storeSession($sessionData, $tokens);

// Retrieve and validate sessions
$session = $tokenStorage->getSessionByAccessToken($accessToken);
$isValid = $tokenStorage->isSessionValid($accessToken);

// Session management
$tokenStorage->revokeSession($accessToken);
$tokenStorage->revokeAllUserSessions($userUuid);
$cleanedCount = $tokenStorage->cleanupExpiredSessions();
```

### Advanced Session Features

```php
// Query builder for complex session queries
$queryBuilder = $tokenStorage->queryBuilder();
$activeSessions = $queryBuilder
    ->where(['user_uuid' => $userUuid])
    ->where(['is_active' => true])
    ->orderBy(['last_activity' => 'DESC'])
    ->get();

// Session analytics
$analytics = $tokenStorage->getSessionAnalytics($userUuid);
$securityMetrics = $tokenStorage->getSecurityMetrics();

// Bulk operations with transactions
$tokenStorage->setUseTransactions(true);
$result = $tokenStorage->bulkUpdateSessions($updates);
```

## Queue System

Process background jobs with the queue system:

### Dispatching Jobs

```php
use Glueful\Queue\QueueManager;

$queueManager = container()->get(QueueManager::class);

// Dispatch a job
$job = new SendEmailJob($userEmail, $emailData);
$queueManager->push($job);

// Dispatch with delay
$queueManager->later(300, $job); // 5 minutes delay

// Batch jobs
$jobs = [$job1, $job2, $job3];
$batchId = $queueManager->pushBatch($jobs);
```

### Queue Commands

```bash
# Process queue jobs
php glueful queue:work
php glueful queue:work --queue=high-priority

# Process batch jobs
php glueful queue:batch:process

# Monitor queue health
php glueful queue:health
php glueful queue:stats

# Handle failed jobs
php glueful queue:failed
php glueful queue:retry 123
php glueful queue:flush
```

## Making API Requests

Standard HTTP interactions with JWT authentication:

```bash
# Register user
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"john","email":"john@example.com","password":"secret123"}'

# Login to get JWT tokens
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"john","password":"secret123"}'

# Use access token for authenticated requests
curl -X GET http://localhost:8000/api/users \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"

# Create user with validation
curl -X POST http://localhost:8000/api/users \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{"name":"John Doe","email":"john@example.com","role":"user"}'

# Refresh token when access token expires
curl -X POST http://localhost:8000/api/auth/refresh \
  -H "Authorization: Bearer YOUR_REFRESH_TOKEN"
```

## Comprehensive CLI Tools

Glueful provides enterprise-grade CLI tools for all system management:

### System Management

```bash
# System health and diagnostics
php glueful system:check                   # Comprehensive system check
php glueful system:check --production      # Production readiness check
php glueful system:check --fix             # Auto-fix common issues

# Installation and setup
php glueful install                        # Interactive setup wizard
php glueful key:generate                   # Generate secure keys
```

### Database Operations

```bash
# Migration management
php glueful migrate:run                    # Run pending migrations
php glueful migrate:rollback               # Rollback last migration
php glueful migrate:status                 # Check migration status
php glueful migrate:create CreateUsersTable # Create new migration

# Database utilities
php glueful db:status                      # Connection status
php glueful db:pool:health                 # Connection pool health
php glueful db:pool:reset                  # Reset connection pool
php glueful db:fresh                       # Fresh database setup
```

### Performance & Monitoring

```bash
# Performance analysis
php glueful performance:memory             # Memory usage analysis
php glueful performance:benchmark          # Run benchmarks
php glueful performance:profile            # Profile application

# Query optimization
php glueful db:query:profile               # Profile queries
php glueful db:query:analyze               # Analyze performance
php glueful db:query:optimize              # Optimization suggestions
```

### Security Management

```bash
# Security operations
php glueful security:scan                  # Security vulnerability scan
php glueful security:headers:check         # Verify security headers
php glueful security:ratelimit:status      # Rate limit status

# Emergency lockdown
php glueful security:lockdown:enable       # Enable lockdown mode
php glueful security:lockdown:disable      # Disable lockdown mode
```

### Cache Management

```bash
# Cache operations
php glueful cache:clear                    # Clear all cache
php glueful cache:status                   # Cache statistics
php glueful cache:warm                     # Warm cache
php glueful cache:analyze                  # Cache usage analysis

# Edge cache (CDN integration)
php glueful cache:edge:purge               # Purge edge cache
```

### Notification System

```bash
# Notification management
php glueful notifications:send             # Send pending notifications
php glueful notifications:retry            # Retry failed notifications
php glueful notifications:channels         # List available channels
php glueful notifications:metrics          # View metrics
```

### Archive System

```bash
# Data archival
php glueful archive:run                    # Run archival process
php glueful archive:restore 12345          # Restore archived data
php glueful archive:stats                  # Archive statistics
php glueful archive:cleanup                # Clean old archives
```

### API Documentation

```bash
# Generate documentation
php glueful generate:json doc              # Generate API docs
php glueful generate:json api-definitions  # Generate OpenAPI specs
```

## Admin Interface

The Glueful Admin Interface is available as an extension that you can install and enable:

```bash
# Install the Admin extension (if not already included)
php glueful extensions:install Admin

# Enable the Admin extension
php glueful extensions:enable Admin

# Get extension info
php glueful extensions:info Admin
```

After installation, you can customize the Admin Interface through the extension's configuration file located at `extensions/Admin/config.php`.

The Admin Interface provides:

- Dashboard analytics
- User management
- Extension management
- Configuration settings
- API exploration tools
- Logging and monitoring
- Scheduled task management
- Database management

Access the Admin Interface by navigating to `http://yourapp.com/admin` after installation and enabling the extension.

## API Documentation

Glueful automatically generates API documentation based on your controllers and routes:

```bash
# Generate API definitions and documentation
php glueful generate:json api-definitions

# View documentation at http://yourapp.com/docs
```

The documentation system supports OpenAPI standards and can be customized to fit your project's needs.
