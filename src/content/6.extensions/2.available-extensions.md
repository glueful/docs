---
title: Available Extensions
description: Official extensions available for the Glueful framework
navigation:
  icon: i-lucide-package
---

This section lists and describes the official extensions available for the Glueful framework. Extensions are modular components that add specific functionality to your Glueful application without changing the core codebase.

## Current Extension Status

Based on the current system configuration, here are the available extensions:

| Extension | Status | Type | Version | Description |
|-----------|--------|------|---------|-------------|
| EmailNotification | **Enabled** | Core | 1.0.0 | Provides email notification capabilities using modern SMTP |
| Admin | **Enabled** | Optional | 0.19.0 | Comprehensive admin dashboard UI with Vue.js SPA |
| RBAC | **Enabled** | Optional | 0.27.1 | Modern, flexible role-based access control system |
| SocialLogin | **Disabled** | Optional | 0.18.0 | Social authentication through Google, Facebook, GitHub, Apple |
| BeanstalkdQueue | **Disabled** | Optional | 1.0.0 | Beanstalkd queue driver for high-performance job processing |

Total: **5 extensions** (3 enabled, 1 core, 4 optional)

Use `php glueful extensions:info` to view current status and detailed information.

## EmailNotification

The EmailNotification extension adds email capabilities to the Glueful Notifications system, allowing your application to send transactional emails and notifications.

### Features

- SMTP mail transport with secure TLS/SSL options
- Email templating system
- HTML and plain text email support
- File attachments
- Queue integration for background sending
- Detailed delivery logging

### Configuration

The extension can be configured via environment variables or by editing the extension's `config.php` file:

```php
// In extensions/EmailNotification/config.php
return [
    // SMTP Server Configuration
    'host' => env('MAIL_HOST') ?: 'smtp.example.com',
    'port' => env('MAIL_PORT') ?: 587,
    'username' => env('MAIL_USERNAME') ?: '',
    'password' => env('MAIL_PASSWORD') ?: '',
    'encryption' => env('MAIL_ENCRYPTION') ?: 'tls', // tls, ssl, or null
    'smtp_auth' => true,
    
    // From Address Configuration
    'from' => [
        'address' => env('MAIL_FROM_ADDRESS') ?: 'noreply@example.com',
        'name' => env('MAIL_FROM_NAME') ?: 'Notification System',
    ],
    
    // Reply-To Address Configuration (optional)
    'reply_to' => [
        'address' => env('MAIL_REPLY_TO_ADDRESS') ?: '',
        'name' => env('MAIL_REPLY_TO_NAME') ?: '',
    ],
    
    // Application Name (used in templates)
    'app_name' => env('APP_NAME') ?: 'Glueful Application',
    
    // Default Email Templates Path
    'templates_path' => null,
    
    // Queue Configuration for background processing
    'queue' => [
        'enabled' => true,
        'connection' => 'default',
        'queue' => 'emails',
        'timeout' => 60
    ]
];
```

### Usage

To send emails using the extension:

```php
use Glueful\Notifications\Notification;
use Glueful\Extensions\EmailNotification\EmailChannel;

$notification = new Notification();
$notification
    ->channel(EmailChannel::class)
    ->to('recipient@example.com')
    ->subject('Welcome to Glueful')
    ->template('welcome', [
        'name' => 'John Doe',
        'appName' => 'Your Application'
    ]);

$notification->send();
```

## SocialLogin

The SocialLogin extension adds OAuth authentication capabilities to your Glueful application, allowing users to sign in using popular social platforms.

### Features

- Multiple provider support:
  - Google
  - Facebook
  - GitHub
  - Apple
- Automatic user registration
- Profile synchronization
- Account linking
- Token generation and session management

### Configuration

Each social provider requires specific credentials that you need to obtain from the respective developer portals. These can be set via environment variables:

```
# Google
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=https://yourdomain.com/auth/social/google/callback

# Facebook
FACEBOOK_APP_ID=your-app-id
FACEBOOK_APP_SECRET=your-app-secret
FACEBOOK_REDIRECT_URI=https://yourdomain.com/auth/social/facebook/callback

# GitHub
GITHUB_CLIENT_ID=your-client-id
GITHUB_CLIENT_SECRET=your-client-secret
GITHUB_REDIRECT_URI=https://yourdomain.com/auth/social/github/callback

# Apple
APPLE_CLIENT_ID=your-services-id
APPLE_CLIENT_SECRET=path-to-your-private-key.p8
APPLE_TEAM_ID=your-team-id
APPLE_KEY_ID=your-key-id
APPLE_REDIRECT_URI=https://yourdomain.com/auth/social/apple/callback
```

You can customize behavior through the extension's `config.php` file:

```php
// In extensions/SocialLogin/config.php
return [
    // General settings
    'enabled_providers' => ['google', 'facebook', 'github', 'apple'],
    'auto_register' => true,  // Automatically create user accounts
    'link_accounts' => true,  // Allow linking social accounts to existing users
    'sync_profile' => true,   // Sync profile data from social providers
    
    // Provider-specific settings
    'google' => [
        'client_id' => env('GOOGLE_CLIENT_ID', ''),
        'client_secret' => env('GOOGLE_CLIENT_SECRET', ''),
        'redirect_uri' => env('GOOGLE_REDIRECT_URI', ''),
    ],
    
    // Facebook OAuth settings
    'facebook' => [
        'app_id' => env('FACEBOOK_APP_ID', ''),
        'app_secret' => env('FACEBOOK_APP_SECRET', ''),
        'redirect_uri' => env('FACEBOOK_REDIRECT_URI', ''),
    ],
    
    // GitHub OAuth settings
    'github' => [
        'client_id' => env('GITHUB_CLIENT_ID', ''),
        'client_secret' => env('GITHUB_CLIENT_SECRET', ''),
        'redirect_uri' => env('GITHUB_REDIRECT_URI', ''),
    ],
    
    // Apple OAuth settings
    'apple' => [
        'client_id' => env('APPLE_CLIENT_ID', ''),
        'client_secret' => env('APPLE_CLIENT_SECRET', ''),
        'team_id' => env('APPLE_TEAM_ID', ''),
        'key_id' => env('APPLE_KEY_ID', ''),
        'redirect_uri' => env('APPLE_REDIRECT_URI', ''),
    ],
];
```

### Usage

Add social login buttons to your login page:

```html
<a href="/auth/social/google" class="btn-google">Sign in with Google</a>
<a href="/auth/social/facebook" class="btn-facebook">Sign in with Facebook</a>
<a href="/auth/social/github" class="btn-github">Sign in with GitHub</a>
<a href="/auth/social/apple" class="btn-apple">Sign in with Apple</a>
```

Handling the login in your controller:

```php
use Glueful\Extensions\SocialLogin;

class AuthController {
    public function handleSocialLogin(string $provider) {
        // Route: /auth/social/{provider}
        $authUrl = SocialLogin::getAuthorizationUrl($provider);
        redirect($authUrl);
    }
    
    public function handleCallback(string $provider) {
        // Route: /auth/social/{provider}/callback
        $token = SocialLogin::processCallback($provider);
        
        if ($token) {
            // User is now authenticated
            // Redirect to dashboard or home page
            redirect('/dashboard');
        }
    }
}
```

## Admin

The Admin Dashboard extension provides a comprehensive management interface built as a Vue.js Single Page Application (SPA). This extension is currently **enabled** and features modern UI components.

### Features

- **Vue.js SPA Interface**: Modern, responsive dashboard with real-time updates
- **System Health Monitoring**: CPU, memory, disk usage, and database metrics
- **Database Management**: Visual query builder, table management, and migrations
- **API Testing Tools**: Interactive API explorer and testing interface
- **Extension Management**: Enable/disable extensions, view health status
- **User & Role Management**: Complete RBAC integration when RBAC extension is enabled
- **Real-time Metrics**: Live charts and performance analytics
- **Dark/Light Theme**: User preference-based theming

### SPA Configuration

The Admin extension uses SPA configuration in its `manifest.json`:

```json
{
  "spa": {
    "enabled": true,
    "configurations": [
      {
        "path_prefix": "/ui/api/admin",
        "build_path": "public/index.html",
        "name": "Admin Panel",
        "framework": "vue",
        "auth_required": false
      }
    ]
  }
}
```

### Access

Access the admin dashboard at:
- **SPA Interface**: `/ui/api/admin`
- **API Endpoints**: Various `/admin/*` routes for data access

### Built Assets

The Admin extension includes pre-built Vue.js assets:
- **Entry Point**: `public/index.html`
- **JavaScript**: `public/assets/*.js` (Vue components, routing, state management)
- **CSS**: `public/assets/*.css` (Tailwind CSS, component styles)
- **Static Assets**: Favicon, environment configuration

### Integration

The Admin Dashboard integrates with:
- **Extension System**: Real-time extension management and health monitoring
- **RBAC Extension**: Role-based access control when available
- **Database Layer**: Direct database statistics and query execution
- **API Framework**: Complete API testing and exploration tools

## RBAC (Role-Based Access Control)

The RBAC extension provides comprehensive role and permission management for Glueful applications. This extension is currently **enabled**.

### Features

- Role creation and management
- Permission assignment and inheritance
- User role assignment
- Resource-based permissions
- Dynamic permission checking
- Role hierarchies
- Permission caching for performance
- Audit logging for permission changes

### Core Concepts

1. **Roles**: Named collections of permissions (e.g., admin, editor, viewer)
2. **Permissions**: Specific abilities to perform actions (e.g., user.create, post.edit)
3. **Resources**: Protected entities in your application
4. **Actions**: Operations that can be performed on resources

### API Integration

RBAC automatically protects API endpoints when enabled:

```php
// Routes are automatically protected based on permissions
Router::post('/users', [UserController::class, 'create'])
    ->requiresPermission('user.create');

Router::put('/posts/{id}', [PostController::class, 'update'])
    ->requiresPermission('post.edit');
```

### Configuration

Configure RBAC through its `config.php`:

```php
// In extensions/RBAC/config.php
return [
    'enabled' => true,
    
    // Default roles
    'default_roles' => [
        'admin' => [
            'description' => 'System administrator',
            'permissions' => ['*'] // All permissions
        ],
        'user' => [
            'description' => 'Regular user',
            'permissions' => ['profile.view', 'profile.edit']
        ]
    ],
    
    // Permission inheritance
    'role_hierarchy' => [
        'super-admin' => ['admin'],
        'admin' => ['moderator'],
        'moderator' => ['user']
    ],
    
    // Caching
    'cache_permissions' => true,
    'cache_ttl' => 3600, // 1 hour
];
```

### Usage

Check permissions in your code:

```php
use Glueful\Extensions\RBAC\RBAC;

// Check if user has permission
if (RBAC::userHasPermission($userId, 'post.create')) {
    // Allow post creation
}

// Check multiple permissions
if (RBAC::userHasAnyPermission($userId, ['post.edit', 'post.delete'])) {
    // Allow post management
}

// Assign role to user
RBAC::assignRole($userId, 'editor');

// Get user roles
$roles = RBAC::getUserRoles($userId);
```

### Database Schema

RBAC creates the following tables:
- `rbac_roles` - Role definitions
- `rbac_permissions` - Permission definitions
- `rbac_role_permissions` - Role-permission mappings
- `rbac_user_roles` - User-role assignments
- `rbac_permission_cache` - Permission cache (optional)

## Extension Categories

Glueful extensions are categorized into tiers based on their importance to the system:

### Core Extensions

Core extensions provide essential functionality that the framework relies on for basic operations. These include:

- **EmailNotification**: Email sending capabilities for system notifications and alerts

Core extensions receive special treatment:
- Enabled automatically in production environments
- Require confirmation before disabling
- Have additional safeguards against accidental removal

### Optional Extensions

Optional extensions add useful but non-essential functionality:

- **SocialLogin**: OAuth authentication through social providers
- **Admin**: Administration interface

Optional extensions can be freely enabled or disabled based on your application's needs.

## Installing Extensions

### Method 1: Using CLI Commands (Recommended)

The easiest way to install and manage extensions is through the CLI:

```bash
# Install from a URL
php glueful extensions:install https://example.com/extension.zip

# Install from a local archive
php glueful extensions:install /path/to/extension.tar.gz

# Enable an installed extension
php glueful extensions:enable ExtensionName

# Verify installation
php glueful extensions:info ExtensionName
```

### Method 2: Manual Installation

1. **Place extension files** in the `extensions/` directory
2. **Run validation** to ensure proper structure:
   ```bash
   php glueful extensions:validate ExtensionName
   ```

3. **Enable the extension**:
   ```bash
   php glueful extensions:enable ExtensionName
   ```

4. **Verify configuration** is updated:
   ```bash
   php glueful extensions:validate
   ```

### Post-Installation Steps

After installing any extension:

1. **Run migrations** if the extension includes database changes:
   ```bash
   php glueful migrate run
   ```

2. **Clear cache** to ensure proper loading:
   ```bash
   php glueful cache clear
   ```

3. **Check extension health**:
   ```bash
   php glueful extensions:info ExtensionName
   ```

4. **Regenerate API documentation** if the extension adds routes:
   ```bash
   php glueful generate:json api-definitions
   ```

### Verifying Installation

Use these commands to verify successful installation:

```bash
# Check if extension appears in list
php glueful extensions:info

# View detailed information
php glueful extensions:info ExtensionName

# Run system diagnostics
php glueful extensions:debug
```

### Tier-Specific Installation Considerations

When installing extensions, be aware of tier-specific considerations:

#### Core Extensions
- Should be installed in all environments
- Require thorough testing before deployment
- May affect system stability if misconfigured

#### Optional Extensions
- Can be installed selectively based on needs
- May be environment-specific (e.g., DevTools for development only)
- Should check dependencies on core extensions

## Managing Extensions

### CLI Management Commands

The extension system v2.0 provides comprehensive management commands:

```bash
# Get extension system information and list all extensions
php glueful extensions:info

# Get detailed information about a specific extension
php glueful extensions:info ExtensionName

# Enable an extension
php glueful extensions:enable ExtensionName

# Disable an extension
php glueful extensions:disable ExtensionName

# Force disable a core extension (use with caution)
php glueful extensions:disable CoreExtensionName --force

# Delete an extension (with confirmation)
php glueful extensions:delete ExtensionName

# Validate extension structure
php glueful extensions:validate ExtensionName
```

### System Monitoring Commands

Monitor and debug your extension system:

```bash
# Run performance benchmarks
php glueful extensions:benchmark

# Get comprehensive debug information
php glueful extensions:debug

# Validate extension configuration
php glueful extensions:validate
```

### REST API Management

Manage extensions programmatically via the REST API:

```bash
# List extensions
curl -X GET http://your-app.com/extensions \
  -H "Authorization: Bearer YOUR_TOKEN"

# Enable extension
curl -X POST http://your-app.com/extensions/enable \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name": "ExtensionName"}'

# Get extension health
curl -X POST http://your-app.com/extensions/ExtensionName/health \
  -H "Authorization: Bearer YOUR_TOKEN"

# View dependency graph
curl -X GET http://your-app.com/extensions/dependencies \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### Managing Extensions Through the Admin Interface

The Admin Dashboard extension provides a visual interface for managing extensions, with special indicators for core vs. optional extensions:

- Core extensions are displayed with a "Core" badge
- Tier-specific warnings appear when attempting to disable core extensions
- Dependency visualization shows tier relationships between extensions
- Color-coding helps identify extension tiers at a glance

### Extension Health and Diagnostics

All Glueful extensions implement a `checkHealth()` method that provides diagnostic information about the extension's status. You can check extension health through the CLI or Admin interface:

```bash
# Check extension health through CLI
php glueful extensions health ExtensionName
```

The health check provides information about:
- Whether the extension is functioning correctly
- Any issues or warnings
- Resource usage metrics (memory, execution time, etc.)
- Dependency status

### Extension Environment Compatibility

Extensions can specify their compatibility with different environments (development, staging, production) through the `isEnabledForEnvironment()` method. This allows extensions to be automatically enabled or disabled based on the current environment.

```php
// In your extension class
public static function isEnabledForEnvironment(string $environment): bool
{
    // Only enable in development environment
    return $environment === 'development';
}
```

## Performance Considerations

The extension system v2.0 is optimized for high performance:

### Loading Performance
- **Sub-millisecond loading**: Extensions load in <1ms
- **Pre-computed metadata**: No filesystem scanning required
- **Minimal memory usage**: ~26KB for typical setup

### Monitoring Tools
```bash
# Benchmark extension loading
php glueful extensions benchmark

# Check memory usage
php glueful extensions debug
```

### Best Practices for Performance
1. **Keep extensions lightweight**: Minimize initialization code
2. **Use lazy loading**: Load resources only when needed
3. **Cache configuration**: Leverage the built-in caching system
4. **Monitor regularly**: Use benchmark command to track performance
5. **Validate dependencies**: Ensure no circular dependencies

## Troubleshooting Extensions

### Common Issues and Solutions

1. **Extension not appearing in list**
   - Run `php glueful extensions validate-config`
   - Check if extension is in `extensions/` directory
   - Verify JSON configuration is valid

2. **Extension fails to enable**
   - Check dependencies: `php glueful extensions info ExtensionName`
   - Review logs for specific errors
   - Validate structure: `php glueful extensions validate ExtensionName`

3. **Performance degradation**
   - Run benchmarks: `php glueful extensions benchmark`
   - Check debug output: `php glueful extensions debug`
   - Review extension's resource usage

4. **Namespace conflicts**
   - View registered namespaces: `php glueful extensions namespaces`
   - Ensure unique namespace paths in extensions.json
